package com.example.chaircharge.network

object ApiConfig {
    // 같은 Wi-Fi 환경에서 PC FastAPI 서버에 접속하기 위한 주소입니다.
    // FastAPI 서버는 --host 0.0.0.0 --port 8000 옵션으로 실행해야 합니다.
    const val BASE_URL = "http://172.30.1.39:8000/"
}
